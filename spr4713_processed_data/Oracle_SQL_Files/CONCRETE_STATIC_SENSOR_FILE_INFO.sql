CREATE TABLE CONCRETE_STATIC_SENSOR_FILE_INFO (
id NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
filename VARCHAR2(100 BYTE) NOT NULL,
scan_point VARCHAR2(20 BYTE) NOT NULL,
starting_timestamp DATE NOT NULL,
PRIMARY KEY(id)
);